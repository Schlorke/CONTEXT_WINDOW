// Facade over the design system: shared/ui never defines a second component source.
export { Button, Stack, Text, ThemeProvider, catalog } from "@acme/ui";
export type { ButtonProps, StackProps, TextProps } from "@acme/ui";
